(function(){'use strict';const U='https://aserkyiwwyggtixckjsv.supabase.co',K='sb_publishable_7THOazCrwgQGvRPGC8grgA_6J1E_9HX';async function run(){try{const m=await import('https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm'),db=m.createClient(U,K),r=await db.from('site_media_slides').select('*').in('placement',['hero','banner']).eq('is_active',true).order('sort_order');if(r.error||!r.data?.length)return;const slides=r.data.filter(x=>x.placement==='hero'),banners=r.data.filter(x=>x.placement==='banner');if(slides.length){const hero=document.querySelector('.hero');if(hero){const box=document.createElement('div');box.id='stage5Hero';box.innerHTML=slides.map((x,i)=>`<a class="s5-slide ${i?'':'on'}" href="${x.link_url||'#'}"><img src="${x.image_url}" alt="${x.title||'مرکز مشاوره تلفنی روان'}"><div class="s5-caption"><b>${x.title||''}</b><span>${x.subtitle||''}</span>${x.button_text?`<em>${x.button_text}</em>`:''}</div></a>`).join('')+`<div class="s5-dots">${slides.map((_,i)=>`<button data-i="${i}"></button>`).join('')}</div>`;hero.parentNode.insertBefore(box,hero);let i=0;const show=n=>{i=(n+slides.length)%slides.length;box.querySelectorAll('.s5-slide').forEach((e,j)=>e.classList.toggle('on',j===i));box.querySelectorAll('.s5-dots button').forEach((e,j)=>e.classList.toggle('on',j===i))};box.querySelectorAll('.s5-dots button').forEach(b=>b.onclick=()=>show(+b.dataset.i));show(0);if(slides.length>1)setInterval(()=>show(i+1),6000)}}if(banners.length){const x=banners[0],b=document.createElement('a');b.className='s5-banner';b.href=x.link_url||'#';b.innerHTML=`<img src="${x.image_url}" alt="${x.title||'بنر'}"><span><b>${x.title||''}</b>${x.subtitle?`<small>${x.subtitle}</small>`:''}</span>`;document.querySelector('.section')?.prepend(b)}}catch(e){console.warn('homepage media',e)}}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',run);else run()})();