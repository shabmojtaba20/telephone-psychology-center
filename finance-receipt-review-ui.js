(()=>{
'use strict';
const $=id=>document.getElementById(id);
const escLocal=s=>String(s??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));
const faDate=v=>{if(!v)return '—';const d=new Date(v);return Number.isNaN(d.getTime())?'—':d.toLocaleString('fa-IR-u-ca-persian',{year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit'});};
const money=n=>Number(n||0).toLocaleString('fa-IR');
function modal(){let m=$('receiptReviewModal');if(m)return m;m=document.createElement('div');m.id='receiptReviewModal';m.innerHTML=`<div class="rr-backdrop"></div><div class="rr-card"><button class="rr-close" type="button">×</button><h2>🧾 بررسی رسید کارت‌به‌کارت</h2><div id="rrMeta" class="rr-meta"></div><div id="rrFile" class="rr-file"></div><div id="rrActions"></div></div>`;document.body.appendChild(m);m.querySelector('.rr-close').onclick=()=>m.remove();m.querySelector('.rr-backdrop').onclick=()=>m.remove();return m}
function tempField(id,value){let old=$(id);if(old)old.remove();const el=document.createElement('input');el.type='hidden';el.id=id;el.value=value||'';document.body.appendChild(el);return el}
async function openReview(id,path,status,name,phone,amount,tracking,submittedAt){const m=modal();m.querySelector('#rrMeta').innerHTML='<div><b>کاربر صادرکننده:</b> '+escLocal(name||'کاربر')+'</div><div><b>شماره تماس:</b> '+escLocal(phone||'—')+'</div><div><b>مبلغ:</b> '+money(amount)+' تومان</div><div><b>کد پیگیری:</b> '+escLocal(tracking||'—')+'</div><div><b>تاریخ ثبت:</b> '+escLocal(faDate(submittedAt))+'</div><div><b>وضعیت:</b> '+escLocal(status||'در انتظار')+'</div>';const box=m.querySelector('#rrFile');box.innerHTML='<div class="rr-loading">در حال بارگذاری رسید...</div>';if(path){const r=await db.storage.from('payment-receipts').createSignedUrl(path,300);if(r.error)box.innerHTML='<div class="rr-error">نمایش رسید ممکن نیست: '+escLocal(r.error.message)+'</div>';else{const u=r.data?.signedUrl||'';const lower=path.toLowerCase();box.innerHTML=lower.endsWith('.pdf')?`<iframe class="rr-pdf" src="${u}" title="رسید پرداخت"></iframe>`:`<img class="rr-img" src="${u}" alt="رسید پرداخت">`;}}else box.innerHTML='<div class="rr-error">برای این پرداخت فایل رسید ثبت نشده است.</div>';const actions=m.querySelector('#rrActions');actions.innerHTML='';if(status==='pending'){actions.innerHTML=`<div class="rr-review"><input id="rrNote" placeholder="یادداشت یا علت رد"><input id="rrTrack" placeholder="کد پیگیری نهایی"><button class="btn green" id="rrApprove">تأیید رسید</button><button class="btn red" id="rrReject">رد رسید</button></div>`;const submit=async decision=>{tempField('note_'+id,$('rrNote')?.value||'');tempField('track_'+id,$('rrTrack')?.value||tracking||'');try{await window.reviewReceipt(id,decision)}finally{const a=$('note_'+id),b=$('track_'+id);if(a)a.remove();if(b)b.remove();m.remove()}};$('rrApprove').onclick=()=>submit('approved');$('rrReject').onclick=()=>submit('rejected')}}
async function loadReceiptsSimplified(){const f=$('receiptFilter')?.value||'pending';let q=db.from('payment_receipts').select('id,user_id,amount,status,submitted_at,receipt_path,tracking_code,payer_card_last4,admin_note').order('submitted_at',{ascending:false}).limit(100);if(f!=='all')q=q.eq('status',f);const r=await q;if(r.error){$('receiptsBody').innerHTML='<tr><td colspan="8">'+escLocal(r.error.message)+'</td></tr>';return}const rows=r.data||[];const ids=[...new Set(rows.map(x=>x.user_id).filter(Boolean))];let profiles={};if(ids.length){const p=await db.from('user_profiles').select('user_id,full_name,phone').in('user_id',ids);(p.data||[]).forEach(x=>profiles[x.user_id]={name:x.full_name||'—',phone:x.phone||'—'})}$('receiptsBody').innerHTML=rows.length?rows.map(x=>{const profile=profiles[x.user_id]||{name:'کاربر',phone:'—'};const pending=x.status==='pending';const name=profile.name,phone=profile.phone,tracking=x.tracking_code||'—';return `<tr class="${pending?'pending-row':''}"><td>${escLocal(name)}</td><td>${escLocal(phone)}</td><td>${money(x.amount)} تومان</td><td>${escLocal(tracking)}${x.payer_card_last4?' / ****'+escLocal(x.payer_card_last4):''}</td><td><span class="badge">${escLocal(x.status)}</span></td><td>${faDate(x.submitted_at)}</td><td>${x.receipt_path?`<button class="btn gray" onclick="window.__reviewReceipt('${escLocal(x.id)}','${escLocal(x.receipt_path)}','${escLocal(x.status)}','${escLocal(name)}','${escLocal(phone)}','${escLocal(x.amount)}','${escLocal(tracking)}','${escLocal(x.submitted_at)}')">${pending?'بررسی و مشاهده رسید':'مشاهده رسید'}</button>`:'—'}</td><td>${pending?'—':escLocal(x.admin_note||'بررسی شده')}</td></tr>`}).join(''):'<tr><td colspan="8" class="empty">رسیدی یافت نشد.</td></tr>'}
function install(){if(!window.db||!$('receiptsBody')||window.__receiptReviewInstalled)return;window.__receiptReviewInstalled=true;window.__reviewReceipt=openReview;const table=$('receiptsBody')?.closest('table');if(table){const thead=table.querySelector('thead');if(thead)thead.innerHTML='<tr><th>کاربر صادرکننده</th><th>شماره تماس</th><th>مبلغ</th><th>کد پیگیری</th><th>وضعیت</th><th>تاریخ ثبت</th><th>رسید</th><th>نتیجه بررسی</th></tr>';}window.loadReceipts=loadReceiptsSimplified;loadReceiptsSimplified();const style=document.createElement('style');style.textContent=`.rr-backdrop{position:fixed;inset:0;background:rgba(15,23,42,.58);z-index:10000}.rr-card{position:fixed;z-index:10001;top:4vh;left:50%;transform:translateX(-50%);width:min(760px,94vw);max-height:92vh;overflow:auto;background:#fff;border-radius:18px;padding:20px;box-shadow:0 25px 70px rgba(0,0,0,.25)}.rr-close{position:absolute;left:12px;top:10px;border:0;background:#f1f5f9;border-radius:50%;width:34px;height:34px;font-size:25px;cursor:pointer}.rr-meta{background:#f8fafc;border-radius:10px;padding:10px;margin:10px 0;line-height:2}.rr-file{text-align:center;min-height:80px}.rr-img{max-width:100%;max-height:58vh;object-fit:contain;border-radius:10px;border:1px solid #e5e7eb}.rr-pdf{width:100%;height:58vh;border:1px solid #e5e7eb;border-radius:10px}.rr-review{display:flex;gap:8px;flex-wrap:wrap;margin-top:14px}.rr-review input{flex:1;min-width:170px;padding:10px;border:1px solid #d6dbe5;border-radius:9px}.rr-loading{padding:30px;color:#667085}.rr-error{padding:18px;background:#fff1f2;color:#9f1239;border-radius:10px}`;document.head.appendChild(style)}
let tries=0;function boot(){if(window.loadReceipts&&window.db&&$('receiptsBody'))return install();if(++tries<80)setTimeout(boot,100)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();